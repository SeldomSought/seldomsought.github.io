/**
 * The completion transition's copy — a quiet synthesis moment between the
 * last answered item and the results report, not a celebration. See
 * components/journey/CompletionTransition.tsx.
 */
export const COMPLETION_COPY = {
  thesis: 'The map is not the territory.',
  // A borrowed line, not this app's own — credited quietly rather than
  // left to read as if it were, at the one moment in the product weighted
  // heavily enough for that to matter.
  thesisAttribution: '— after Alfred Korzybski',
  reveal: 'But patterns have emerged.',
  cta: 'Explore Your Profile',
  skip: 'Skip',
}
